{
  "name": "severally-mcp",
  "version": "1.1.0",
  "description": "MCP server that lets Codex, Claude Code and Antigravity ask each other for independent opinions, reviews and structured debate via dedicated, sandboxed child sessions.",
  "type": "module",
  "license": "MIT",
  "private": false,
  "bin": {
    "severally-mcp": "bin/severally-mcp.mjs"
  },
  "files": [
    "bin",
    "src",
    "plugins",
    "scripts",
    ".agents",
    "README.md"
  ],
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "test": "node scripts/test.mjs",
    "live-check": "node scripts/live-check.mjs",
    "install-all": "node scripts/install.mjs",
    "live-mcp-check": "node scripts/live-mcp-check.mjs",
    "build": "node scripts/build.mjs",
    "init-config": "node scripts/init-config.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "cross-spawn": "^7.0.6",
    "which": "^2.0.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
